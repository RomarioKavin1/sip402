import type { NextConfig } from "next";

// The marketing site is fully static (hero + docs). No backend, no @sip402
// runtime deps — deploy anywhere (Vercel, Netlify, any static host).
const nextConfig: NextConfig = {};

export default nextConfig;
